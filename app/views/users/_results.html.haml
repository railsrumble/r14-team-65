%h3 Results:

- gists.each do |gist|
  %div.result
    %h4
      = link_to gist.title, gist_path(gist)
    %small= " Updated #{distance_of_time_in_words(gist.last_update, Time.now)} ago"
    .tags
      %input.gist-input-tags(type="text" value="#{gist.tag_list}" data-gistid ="#{gist.id}")
    .description
      = gist.description
